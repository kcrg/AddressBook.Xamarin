<Shell x:Class="AddressBook.Views.AppShell"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:AddressBook.Views"
    Shell.BackgroundColor="White"
    Shell.FlyoutBehavior="Disabled"
    Shell.ForegroundColor="Black"
    Shell.TabBarUnselectedColor="DarkGray"
    Shell.TitleColor="Black"
    Visual="Material">
    <TabBar>
        <Tab Title="Contacts">
            <Tab.Icon>
                <OnPlatform x:TypeArguments="FileImageSource">
                    <On Platform="Android" Value="icon_contacts_list.xml"/>
                    <On Platform="iOS" Value=""/>
                    <On Platform="UWP" Value=""/>
                </OnPlatform>
            </Tab.Icon>

            <ShellContent ContentTemplate="{DataTemplate views:ContactsListPage}"/>
        </Tab>

        <Tab Title="About">
            <Tab.Icon>
                <OnPlatform x:TypeArguments="FileImageSource">
                    <On Platform="Android" Value="icon_info.xml"/>
                    <On Platform="iOS" Value=""/>
                    <On Platform="UWP" Value=""/>
                </OnPlatform>
            </Tab.Icon>

            <ShellContent ContentTemplate="{DataTemplate views:AboutPage}"/>
        </Tab>
    </TabBar>
</Shell>